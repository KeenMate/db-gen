// Code generated by db-gen LOCAL; DO NOT EDIT.
//
// Bulk COPY loader for dbgen_test.copy_target_demo.
// Source fields are passed as strings in table-ordinal order; this matches the
// "stage everything as text, cast server-side" pattern. For a typed stage table,
// switch the Values() entries to convert using each column's DbColumnType.
package generated

import (
	"context"

	"github.com/jackc/pgx/v5"
)

// DbgenTestCopyTargetDemoColumns is the COPY column list, in wire order:
// context columns first, then data columns. Kept in lockstep with the table by db-gen.
var DbgenTestCopyTargetDemoColumns = []string{
	"created_by", // text (context: ctx.CreatedBy)
	"job_run_id", // int8 (context: ctx.JobRunId)
	"company_id", // text
	"name", // text
	"amount", // text
}

// DbgenTestCopyTargetDemoSource streams data rows into the COPY. Each call to Next
// fills 3 fields, in table-ordinal order.
type DbgenTestCopyTargetDemoSource interface {
	Next(fields *[]string) bool
	Err() error
}

// CopyDbgenTestCopyTargetDemo bulk-loads rows into dbgen_test.copy_target_demo via COPY.
// Context columns are supplied once and injected into every row.
func CopyDbgenTestCopyTargetDemo(
	ctx context.Context,
	conn *pgx.Conn,
	src DbgenTestCopyTargetDemoSource,
	createdBy string,
	jobRunId int64,
) (int64, error) {
	cs := &dbgenTestCopyTargetDemoCopySource{
		src: src,
		createdBy: createdBy,
		jobRunId: jobRunId,
	}
	return conn.CopyFrom(
		ctx,
		pgx.Identifier{"dbgen_test", "copy_target_demo"},
		DbgenTestCopyTargetDemoColumns,
		cs,
	)
}

// dbgenTestCopyTargetDemoCopySource adapts a DbgenTestCopyTargetDemoSource to pgx.CopyFromSource.
type dbgenTestCopyTargetDemoCopySource struct {
	src DbgenTestCopyTargetDemoSource
	cur []string
	createdBy string
	jobRunId int64
}

func (s *dbgenTestCopyTargetDemoCopySource) Next() bool { return s.src.Next(&s.cur) }
func (s *dbgenTestCopyTargetDemoCopySource) Err() error { return s.src.Err() }

func (s *dbgenTestCopyTargetDemoCopySource) Values() ([]any, error) {
	return []any{
		s.createdBy, // created_by
		s.jobRunId, // job_run_id
		s.val(0, false), // company_id text
		s.val(1, true), // name text
		s.val(2, true), // amount text
	}, nil
}

// val returns nil for an empty nullable field (the COPY "NULL ''" intent), else the raw text.
func (s *dbgenTestCopyTargetDemoCopySource) val(i int, nullable bool) any {
	if nullable && s.cur[i] == "" {
		return nil
	}
	return s.cur[i]
}
